# Build output
/dialogmapper
/dist/
web/node_modules/
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/

# Local project data created by `dialogmapper init` during development
maps.db
maps.db-wal
maps.db-shm

# internal/web/dist is intentionally NOT ignored: it is committed so that
# `go build` and `go install` work without a Node toolchain.

.DS_Store
*.log
*.timestamp-*.mjs
